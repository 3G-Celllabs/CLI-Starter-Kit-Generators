import packageInfo from "../../package.json";

export const environment = {
  production: false,
};

export const RK_KEY_BASE64 = "";
export const RK_IV_BASE64 = "";

export const BASE_URL = "";

export const MQTT_URL = "";

export const APP_VERSION = packageInfo.version;
